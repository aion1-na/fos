# OECD Public Context

Canonical dataset name: `oecd_public_context`

Access status: request-status stub until indicator selection, license, and vintage are recorded.

License metadata: OECD public-data license and attribution requirements must be captured before production ingestion.

Codebook mapping: `codebooks/oecd_public_context.yaml` maps selected country-year indicators into public international context.

Quality profile: production profiles must document indicator definitions, country coverage, revisions, missingness, and comparability limitations.

Provenance manifest: connector metadata only until public indicator extracts are staged with content hashes.

Access policy: public aggregate indicators only.

Limitations: country-level aggregates may not describe subnational or individual experience.

Inappropriate uses: do not treat contextual indicators as causal effects or prescriptive recommendations.
