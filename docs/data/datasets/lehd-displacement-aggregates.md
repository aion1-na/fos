# LEHD displacement aggregates

Status: request_status_stub.

License metadata: Census RDC restricted-use terms pending. No raw LEHD data is stored in FOS.

Codebook mapping: Intended aggregate mappings only: displacement event rates, reemployment latency, and wage trajectory bins.

Quality profile: Pending access and disclosure review. Any future aggregate result must include cell suppression and re-identification review metadata.

Provenance manifest: `docs/data/tier3/secure-analysis-manifest.schema.json` and future secure analysis manifest hash.

Access policy: Tier 3 restricted source. Only approved aggregate outputs may enter FOS. Public use requires publication path approval.

Limitations: Request-status metadata only. Not a production dataset and not a source of current model calibration.
