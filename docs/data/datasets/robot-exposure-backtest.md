# Dataset Card: Robot Exposure Backtest

Canonical dataset name: `robot_exposure_backtest`

Version: `fixture-0.1`

Access status: fixture-only parser for unit tests

License metadata: pending source review; do not promote fixture-only rows to production artifacts

Codebook mapping: `codebooks/acemoglu_restrepo_robot_exposure.yaml`; parser preserves source occupation codes

Quality profile: fixture-only tests verify provenance to raw archive hash and source occupation code coverage

Provenance manifest: generated during feature build from approved input bytes

Access policy: no production reads without approved source metadata

Simulation output status: may contribute only as one measure inside `features.occupation_ai_exposure_ensemble`; never as a single-score headline.
