# Dataset Card: GFS Wave 1

Canonical dataset name: `gfs_wave1`

Version: `fixture-0.1`

Access status: fixture; production access is registration-gated through the approved GFS OSF/portal workflow.

License metadata: registration-gated; no production license grant is stored in source control.

Sampling design: cross-sectional Wave 1 survey fixture with respondent-level sampling weights.

Weights: `weight` is required for country marginals and six-domain scoring.

Limitations: cross-sectional, research-grade measurement only. This card does not authorize prospective forecasting claims.

Citation instructions: cite the official GFS Wave 1 methodology and portal/OSF record once access is approved; fixture outputs cite this repository fixture only.

Codebook mapping: `codebooks/gfs_wave1.yaml`

Quality profile: fixture tests compare weighted domain marginals against known reference outputs.

Provenance manifest: fixture content hash computed during tests.

Access policy: no production reads until registration and license metadata are approved.

Simulation output status: feature outputs must include `dataset_reference`.
