# Eurostat Public Context

Canonical dataset name: `eurostat_public_context`

Access status: request-status stub until indicator selection, license, and vintage are recorded.

License metadata: Eurostat public-data license and attribution requirements must be captured before production ingestion.

Codebook mapping: `codebooks/eurostat_public_context.yaml` maps selected public EU indicators into country-year context.

Quality profile: production profiles must document indicator definitions, revisions, missingness, geographic coverage, and comparability limitations.

Provenance manifest: connector metadata only until public indicator extracts are staged with content hashes.

Access policy: public aggregate indicators only.

Limitations: country-level aggregates may not support individual or local claims.

Inappropriate uses: do not use aggregates as causal effects, eligibility scores, or prescriptive recommendations.
