FROM python:3.12-slim

WORKDIR /app

COPY apps/api/pyproject.toml apps/api/pyproject.toml
COPY apps/api/src apps/api/src

RUN pip install --no-cache-dir ./apps/api

EXPOSE 8000

CMD ["uvicorn", "fos_api.main:app", "--host", "0.0.0.0", "--port", "8000"]
