{
  "name": "@fos/atlas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "lint": "node -e \"process.exit(0)\"",
    "fmt": "prettier --check .",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "prettier": "3.3.3",
    "typescript": "5.5.4"
  }
}
