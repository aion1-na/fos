# Render Core

Owner: Visualization Engineering

Test command: `pnpm --filter @fos/render-core test`

Definition of done:

- Render helpers are pack-agnostic.
- Data visualizations expose accessible labels.
- Rendering fixtures contain no production credentials or raw restricted data.
- Type checks and package tests pass.
