[project]
name = "fos-population-synth"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fsspec>=2024.12.0",
  "fw-contracts",
  "numpy>=2.0.0",
  "pyarrow>=18.0.0",
  "pyyaml>=6.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
