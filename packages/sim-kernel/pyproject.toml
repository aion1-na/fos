[project]
name = "fos-sim-kernel"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fw-contracts",
  "numpy>=2.0.0",
  "pyarrow>=18.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
