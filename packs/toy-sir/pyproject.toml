[project]
name = "fos-pack-toy-sir"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fos-population-synth",
  "fos-contracts",
  "pydantic>=2.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
