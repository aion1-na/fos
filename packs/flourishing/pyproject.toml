[project]
name = "fos-pack-flourishing"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fw-contracts",
  "numpy>=2.1.0",
  "pydantic>=2.9.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
