{
  "name": "fos",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev": "pnpm --filter @fos/studio dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "fmt": "pnpm -r fmt",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "5.5.4"
  }
}
